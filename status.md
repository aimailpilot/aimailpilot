# STATUS.md — DO NOT BREAK (100 features confirmed working)

> Do NOT modify code for these features unless explicitly asked to fix a bug in that specific feature.
> When in doubt — **ask before changing**.

## Working Features

| # | Feature | Do Not Touch |
|---|---------|-------------|
| 1 | Email Sending (SMTP + OAuth + throttling + test send) | `campaign-engine.ts`, `smtp-email-service.ts`, `/api/campaigns/send-test` route |
| 2 | Google OAuth (login + add sender) | `google-oauth.ts`, `routes.ts:504-944` |
| 3 | Unified Inbox (Gmail + Outlook sync) | Route order: `/api/inbox/enhanced` and `/api/inbox/stats` BEFORE `/api/inbox/:id` |
| 4 | Campaign Tracking — open/click/reply (LOCKED) | `gmail-reply-tracker.ts`, `outlook-reply-tracker.ts`, tracking logic in `campaign-engine.ts`, `storage.ts` |
| 5 | Contact Management (CRUD + CSV import) | Contact storage methods, CSV import in `oauth-routes.ts` |
| 6 | Template Creation (editor + variables) | Template routes in `routes.ts`, `personalization-engine.ts` |
| 7 | Azure OpenAI Integration | `llm.ts` — endpoint config, API key, model settings |
| 8 | Azure App Deployment | `web.config`, `.deployment`, `startup.sh` |
| 9 | Campaign Detail (large campaigns, 500 cap) | `getCampaignMessagesEnriched`, `getCampaignMessageStats`, Step 0 guarantee block |
| 10 | Template Deliverability (scoring + AI fix) | `/api/templates/analyze-deliverability`, deliverability panel in `template-manager.tsx` |
| 11 | Template Preview (desktop/mobile + test email) | Preview dialog + `sendTestEmail` in `template-manager.tsx` |
| 12 | Template Visibility (public/private) | `getPublicEmailTemplatesExcludingUser`, visibility logic in template routes, `getUserRole` |
| 13 | AI Template Content Insertion | `textToHtml` function in `template-manager.tsx` |
| 14 | Contact List Access Control (member vs admin) | `getContactListsForUser`, role-based filtering in `GET /api/contacts` |
| 15 | Campaign Update Dialog (settings + preview) | Update dialog, autopilot, `trackOpens`/`includeUnsubscribe` in `campaign-detail.tsx` |
| 16 | URL Hash Navigation | Hash-based routing in `mailmeteor-dashboard.tsx` |
| 17 | Outlook OAuth Sending (Graph API) | `sendViaMicrosoftGraph` in `campaign-engine.ts`, Outlook path in `/api/contacts/send-email` |
| 18 | Send Email from Contacts (Write/Template/AI) | Send dialog, `handleAiGenerate`, `applyTemplate` in `contacts-manager.tsx` |
| 19 | Database Safety | See Golden Rules below |
| 20 | Campaign Engine (step delays, scheduling, jitter) | `evaluateFollowupTrigger`, `getNextValidSendTime`, `checkSendingWindow`, `getUserLocalTime` |
| 21 | Follow-up Threading — Gmail + Outlook (CONFIRMED WORKING 2026-04-13) | Gmail: threadId stored at step-0, `threadLinked=true` confirmed in logs. Outlook: `Prefer: IdType="ImmutableId"` on all Graph calls ensures providerMessageId survives Drafts→Sent move; `createReply` path in `followup-engine.ts` uses stored ID to inherit conversationId; `toRecipients` PATCH override fixes self-reply bug. Both confirmed: all steps appear in one conversation thread. |
| 22 | Follow-up Personalization (22+ variables) | `personalData` + `personalizeText()` in `followup-engine.ts` |
| 23 | Follow-up Sender Display Name | `senderDisplayName` in `sendEmail()` in `followup-engine.ts` |
| 24 | Email Subject Encoding (RFC 2047) | `mimeEncodeSubject()` in `campaign-engine.ts` and `followup-engine.ts` |
| 25 | Template Editor Rich Text (lists, fonts, AI bar) | `TbBtn`, `execCmd`, toolbar in `template-manager.tsx` + all editor pages |
| 26 | Import Safety — NEVER import db.ts | `server/db.ts` exists for type gen only. See Golden Rules. |
| 27 | Pipeline & Activity Log | Pipeline/activity routes in `routes.ts:4438-4548`, `contact_activities` table |
| 28 | EmailListVerify Integration | `email-verifier.ts`, verification routes in `routes.ts:9360-9480` |
| 29 | Warmup Engine (send + engage + labels) | `warmup-engine.ts`, warmup routes, `startWarmupEngine()` in `index.ts` |
| 30 | Inbox Reply with Token Refresh | Token refresh + retry in `POST /api/inbox/:id/reply`, draft save |
| 31 | Reply Tracker Per-Org Locking | `checkingOrgs` Set in `gmail-reply-tracker.ts` and `outlook-reply-tracker.ts` |
| 32 | Team Scorecard & Leaderboard | `/api/team/scorecard` route, `team-scorecard.tsx` — all raw SQL must use quoted camelCase + parseInt() on COUNT/SUM |
| 33 | My Dashboard (individual view) | `/api/my/dashboard`, `/api/my/emails-needing-reply`, `my-dashboard.tsx` — all raw SQL must use quoted camelCase + parseInt() on COUNT/SUM |
| 34 | Deal Tracking (value, notes, auto-close) | Deal columns in `storage.ts`, deal dialog in `contacts-manager.tsx` |
| 35 | Follow-up Subject (no Re: when threaded) | Subject logic in `executeFollowup()` in `followup-engine.ts` |
| 36 | Raw SQL Access | `storage.rawGet/rawAll/rawRun` — the ONLY safe way for custom SQL |
| 37 | Warmup Improvements (spam rescue, labels, pairs) | Label/folder functions, spam detection, `sendPairs` in `warmup-engine.ts`, `warmup-monitoring.tsx` |
| 38 | AI Lead Intelligence (scan + classify) | `lead-intelligence-engine.ts`, `email_history`/`lead_opportunities` tables, `lead-opportunities.tsx` |
| 39 | Contact Enrichment (AI badges + smart filters) | Enrichment block in `GET /api/contacts`, `GET /api/contacts/hot-leads`, `LEAD_BUCKET_CONFIG` |
| 40 | Follow-up Bounce Skip | Bounce skip block in `processCampaignFollowups()` in `followup-engine.ts` |
| 41 | Context Engine (Knowledge Base + AI Drafts) | `context-engine.ts`, `org_documents` table, `knowledge-base.tsx` |
| 42 | PostgreSQL Migration (PRODUCTION) | `pg-storage.ts`, `initStorage()`, `DATABASE_URL` env var |
| 43 | Campaign List Pagination (7/page) | Pagination in `mailmeteor-dashboard.tsx`, `/api/campaigns/count` |
| 44 | Bounce Suppression (4-layer protection) | `bounce-sync-engine.ts`, `getSuppressedEmails`, bounced filter in `pg-storage.ts` |
| 68 | Sender Account Blocklist Protection | `bounce-sync-engine.ts` — ALL scanner functions (syncContactsToSuppression, syncInboxToSuppression, syncEmailHistoryToSuppression, scanGmailHistorical, scanOutlookHistorical) MUST call `isSafeToSuppress(email, connectedEmails)` before adding to suppression_list. `getOrgConnectedEmails()` loads email_accounts + warmup_accounts. Missing this guard causes sender accounts to be auto-blocklisted. Same guard MUST exist in `gmail-reply-tracker.ts` and `outlook-reply-tracker.ts` NDR extraction paths. |
| 45 | Contact Activity Log + Pipeline (PG fix) | `routes.ts:4438-4548` — all camelCase MUST be double-quoted for PG |
| 46 | Follow-up Engine — No Mid-Loop Stall | `processMessageFollowups()` in `followup-engine.ts` — uses bulk-loaded message, NO per-contact `getCampaignMessage` re-fetch. Pre-loaded campaign passed from `processCampaignFollowups` → `processMessageFollowups` → `scheduleFollowup`. Removing this causes N×(2-5s) DB stalls with slow PG. |
| 47 | Reply Tracker — 15-min Lookback (not 24h) | `runCheck()` in `gmail-reply-tracker.ts` and `outlook-reply-tracker.ts` — lookback is `15` minutes. Was `1440` (24h) causing continuous DB saturation with 80 accounts. 15-min gives 3× overlap with 5-min poll cycle — no replies missed. Do NOT increase back to 1440. |
| 48 | Follow-up Engine — Paused Campaign Guard | `processScheduledFollowups()` and `executeFollowup()` in `followup-engine.ts` — both check `campaign.status`. If paused/cancelled/draft, execution is deferred (stays `pending`) not skipped — resumes naturally when campaign un-paused. |
| 49 | Follow-up Engine — Overlap Lock | `startFollowupEngine()` in `followup-engine.ts` — `isProcessing` boolean prevents concurrent 30s cycles. If previous cycle still running when next interval fires, it skips and logs. |
| 50 | Follow-up Engine — Atomic Execution Claim | `executeFollowup()` in `followup-engine.ts` — `UPDATE SET status='processing' WHERE status='pending' RETURNING id` atomically claims execution. Null return = already claimed. 5-min stuck-processing recovery in `processFollowupTriggers()`. Do NOT revert to non-atomic read-then-check. |
| 51 | Follow-up Engine — Targeted DB Queries | `processCampaignFollowups()`, `processScheduledFollowups()`, `executeFollowup()` in `followup-engine.ts` — all 50k message fetches replaced with targeted indexed queries for replied/bounced sets and per-contact lookups. Do NOT restore `getCampaignMessages(campaignId, 50000, 0)` in these paths. |
| 52 | Follow-up Engine — Multi-Sequence Completion | `checkFollowupCompletion()` in `followup-engine.ts` — aggregates follow-up steps across ALL sequences for a campaign via `getCampaignFollowups()`. Was reading from one sequence only, causing premature completion. |
| 53 | Follow-up Engine — Account Daily Limit (Atomic) | `executeFollowup()` in `followup-engine.ts` — atomic `UPDATE email_accounts SET dailySent=dailySent+1 WHERE dailySent < dailyLimit RETURNING id` reserves send slot before sending. Decrements on failure. Shares same counter with campaign engine. Do NOT revert to read-then-check pattern. |
| 54 | Follow-up Engine — Idempotency Guard | `executeFollowup()` in `followup-engine.ts` — checks `SELECT 1 FROM messages WHERE campaignId+contactId+stepNumber+status='sent'` before sending. Prevents duplicate sends on crash recovery between send and status update. |
| 55 | Inbox Forward — Full Body + Sender Email | Forward endpoint in `routes.ts` — sends full HTML-formatted body with inline border-left quote styling. Captures `forwardedFrom: msg.fromEmail` so original sender's email is visible in forward. |
| 56 | Inbox Forward Status Indicator | `unified-inbox.tsx` — shows forwarded status card (From/To) after forwarding. `forwardedAt`, `forwardedTo`, `forwardedFrom`, `forwardedBy` columns in `unified_inbox` table (pg-storage.ts). |
| 57 | Inbox Reply Tab (repliedAt filter) | `getInboxMessagesEnhanced`, `getInboxMessageCountEnhanced`, `getInboxStats` in `pg-storage.ts` — Reply tab filter is `(status = 'replied' OR "repliedAt" IS NOT NULL)`. Do NOT revert to `status = 'replied'` only. |
| 58 | Bounce False-Positive Override | `POST /api/inbox/:id/unmark-bounce` route in `routes.ts` — manually unmarks bounced emails. `reply-classifier.ts` requires multiple indicators for non-system senders (system senders always bounce at 95% confidence). |
| 59 | Unified Inbox Mobile Responsive | `unified-inbox.tsx` — fully responsive with Tailwind sm:/md: prefixes. Message list uses compact layout on mobile, desktop-only checkboxes hidden on mobile. |
| 60 | Campaign Dashboard Mobile Responsive | `mailmeteor-dashboard.tsx` — KPI cards 2-col mobile → 4-col desktop. Campaign table switches to card view on mobile (md: breakpoint). Desktop table preserved with full columns. |
| 61 | Campaign List — Sender + List Name | `GET /api/campaigns` in `routes.ts` enriches each campaign with `senderEmail`/`senderName` (from email_accounts), `creatorName`/`creatorEmail` (from users), `listName` (from contact_lists via segmentId or contactIds). Desktop table shows "Sent By" avatar column and "List" badge column. |
| 62 | Reply Classification — Rule-Based + AI | `reply-classifier.ts` with 30+ strengthened patterns (OOO, auto_reply, positive, negative, bounce). `classifyReplyWithAI()` uses Azure OpenAI for borderline `general` cases. `isHumanReply()` helper identifies actionable replies (positive/negative/general/unsubscribe, excludes OOO/auto_reply/bounce). |
| 63 | Inbox Reply Filtering (Exclude Auto-Replies) | `unified_inbox` messages excluded from "Emails Need Reply" if `replyType IN ('ooo', 'auto_reply', 'bounce')`. Only `positive`, `negative`, `general` count as real replies. Boot reclassifier processes NULL/empty/general messages via rules + AI. |
| 64 | Scorecard "Not Replied" — 3-Day Lookback | Scorecard counts contacts emailed **3+ days ago** without reply (gives time to respond). Uses `messages` table `repliedAt IS NULL` check. Short periods (Today/This Week) show 0 or very low numbers as expected. |
| 65 | AI Refine Button — Two-Phase Reclassification | Team Scorecard + My Dashboard get purple "AI Refine" button. Phase 1: rules-based reclassification (5000 msgs). Phase 2: Azure OpenAI batch (200 msgs). Progress message shown; auto-refreshes dashboard after 15s. |
| 66 | Email Rating Engine (Contact Scoring) | `email-rating-engine.ts` — 0–100 score + A+/A/B+…F grade per contact. Batch runs fire-and-forget via `POST /api/contacts/batch-rating`. AI rating uses Azure OpenAI reply-quality scoring. Ratings shown as badges in contacts table and detail card (condition: `emailRatingUpdatedAt` not `emailRating > 0`). Contacts with 0 sent emails correctly score 0/F. |
| 67 | Email Rating — recipientEmail Fallback | `pg-storage.ts getContactEngagementStats` — 3-tier fallback: (1) match by `contactId`, (2) match by `recipientEmail` column (backfilled on startup from contacts), (3) match via old contactIds sharing same email. `messages.recipientEmail` added as `ALTER TABLE` + backfilled via `UPDATE messages SET "recipientEmail" = c.email FROM contacts c WHERE messages."contactId" = c.id`. New messages get `recipientEmail` from `campaign-engine.ts` and `followup-engine.ts`. |
| 69 | Campaign Auto-Pause Recovery (autoPaused flag) — CONFIRMED WORKING 2026-04-14 | `campaign-engine.ts` + `pg-storage.ts` + `routes.ts` — `autoPaused` BOOLEAN column on campaigns distinguishes system pauses (window closed, daily limit, crash → `autoPaused=true`) from user pauses (UI pause/stop → `autoPaused=false`). `resumeActiveCampaigns()` does two-pass boot recovery: (1) `status='active'` rows, (2) `status='paused' AND autoPaused=true AND autopilot.enabled=true`. SQL-filtered, not in-memory. Verified live: 36/36 campaigns ✅ SENDING after deploy + unstick script (2026-04-14). |
| 70 | OOM Crash Prevention — Boot Stagger + Abort on Orphaned ContactIds — CONFIRMED WORKING 2026-04-17 | `campaign-engine.ts` `resumeActiveCampaigns()` — 2s stagger between each `startCampaign()` call in both Pass 1 and Pass 2 prevents simultaneous contact loading from causing heap OOM. Zero-contact fallback (`getContacts(org, 10000, 0)`) replaced with abort + error return when contactIds resolve to 0 contacts — prevents loading 188k contacts. Azure `NODE_OPTIONS=--max-old-space-size=4096` raises heap to 4GB. Do NOT remove stagger or revert fallback to all-org load. |
| 71 | Follow-up Open + Click Tracking (Steps 2/3/4) — FIXED 2026-04-19 | `followup-engine.ts` `executeFollowup()` — `addTrackingPixel()` + `addClickTracking()` + `getBaseUrl()` helpers added to `FollowupEngine` class. `followupTrackingId` generated before send, injected into HTML, stored in message record and tracking event. Step 1 tracking unchanged (in `campaign-engine.ts`). **Root cause was `getBaseUrl()` falling back to `http://localhost:8080` because `BASE_URL` env var not set on Azure** — fixed by adding `_publicBaseUrl` field + `setPublicBaseUrl()` setter (mirrors `CampaignEngine`), and calling `followupEngine.setPublicBaseUrl()` alongside `campaignEngine.setPublicBaseUrl()` in all 7 sites in `routes.ts`. Confirmed working: good_pixel flipped from 0 to 100% at 04:48 UTC 2026-04-19. Do NOT remove `setPublicBaseUrl()` from `FollowupEngine` or the parallel setter calls in `routes.ts`. |
| 72 | Gmail Warmup Leak Fix — CONFIRMED WORKING 2026-04-18 | `gmail-reply-tracker.ts` `checkForReplies()` — builds `ownEmailsSet` from `email_accounts` + `warmup_accounts` once per check cycle, passes to `checkAccountForReplies()`. Per-message: extracts email from `From` header via regex, skips if found in `ownEmailsSet`. Prevents warmup emails (own org accounts) from being stored in unified inbox as `campaign match: false`. Do NOT remove `ownEmailsSet` logic or the `fromEmail` skip check. |
| 73 | Gmail 404 Silent Skip — FIXED 2026-04-18 | `gmail-reply-tracker.ts` `gmailFetch()` — returns `null` on HTTP 404 (message deleted between list and get — benign race condition). Callers check `if (!msg) continue`. Previously threw and retried 2x at 2s/4s causing log noise. 401/429/500 still retry normally. Do NOT remove the 404 null return or the null checks in callers. |
| 74 | PG-SLOW Query Fix — getAllRecentCampaignMessages 90-day cutoff — FIXED 2026-04-18 | `pg-storage.ts` `getAllRecentCampaignMessages()` — added `AND m."sentAt" >= $2` with 90-day cutoff. Lets PostgreSQL use `idx_messages_sent_at` index to skip old rows before JOIN. Reduced query time from 11.6s to ~8s. Do NOT remove the cutoff or change it to all-time. 90 days is sufficient for reply matching. |
| 75 | Replied Tab — Real Replies Across All Accounts — FIXED 2026-04-18 | `pg-storage.ts` `getInboxMessagesEnhanced`, `getInboxMessageCountEnhanced`, `getInboxStats` — Replied tab filter changed from `(status='replied' OR "repliedAt" IS NOT NULL)` to `"replyType" IN ('positive','negative','general')` to show all real incoming replies. Warmup exclusion widened to union `email_accounts ∪ warmup_accounts` and now excludes by sender only (`fromEmail NOT IN orgEmails`). |
| 76 | Warmup Gmail INBOX Leak Fix — FIXED 2026-04-18 | `warmup-engine.ts` `gmailEngage()` — search widened from `is:unread newer_than:1d` to `newer_than:3d` (no unread filter) so engage catches already-read warmup messages. New `warmup-inbox-cleanup.ts` service runs every 2h to backfill-remove INBOX label from messages already tagged AImailPilot-Warmup. First run 5min after boot clears backlog. |
| 78 | WarmupCleanup PG Column Fix — FIXED 2026-04-19 | `warmup-inbox-cleanup.ts` — `getAccessTokenForGmailAccount()` and `getAccessTokenForOutlookAccount()` rewrote to use `storage.getApiSettings(orgId)` with `gmail_sender_{email}_access_token` key pattern (tokens never lived in `email_accounts` columns). Main query removed invalid `WHERE ea."accessToken" IS NOT NULL` filter. Signatures changed to `(orgId, email)`. Callers in `cleanupWarmupInboxOnce()` updated accordingly. Was crashing every 2h with PG error `column ea.accessToken does not exist` since PostgreSQL migration. |
| 77 | Warmup DB Purge — ADDED 2026-04-18 | New `warmup-inbox-purge.ts` service deletes warmup rows from `unified_inbox` older than **2 days** (reduced from 5d 2026-04-19). Safety: requires sender AND recipient both in org email_accounts ∪ warmup_accounts, AND `replyType NOT IN ('positive','negative','general')` — real human replies are never deleted. Runs 10min after boot, then every 6h. |
| 79 | getUnrepliedCampaignMessages 45-day cutoff — FIXED 2026-04-19 | `pg-storage.ts` `getUnrepliedCampaignMessages()` — added `AND m."sentAt" >= $2` with 45-day cutoff. Was scanning all-time messages (no cutoff), causing 8s `[PG-SLOW]` queries. 45d is sufficient for reply tracking; `getAllRecentCampaignMessages` (for reply matching) stays at 90d (different purpose, protected). |
| 80 | Need Reply Inbox Tab — ADDED 2026-04-19 | New `not_replied` filter in `getInboxMessagesEnhanced`, `getInboxMessageCountEnhanced`, `getInboxStats` in `pg-storage.ts` — shows human replies (`replyType IN ('positive','negative','general')`) where `status != 'replied' AND "repliedAt" IS NULL`. Frontend `unified-inbox.tsx` adds "Need Reply" pill tab with count badge. |
| 81 | Reply Quality Scoring (Azure OpenAI) — ADDED 2026-04-19 | New `reply-quality-engine.ts` — scores inbox messages 0–10 via Azure OpenAI (`Hot/Warm/Neutral/Declined/Negative` labels). Columns `replyQualityScore` INTEGER + `replyQualityLabel` TEXT added to `unified_inbox` via `ALTER TABLE IF NOT EXISTS`. Auto-sweep runs every 15min via `server/index.ts` (90s boot delay), scores up to 50 unscored messages per org. Manual trigger via `POST /api/inbox/score-replies`. Rule-based fallback when Azure not configured. |
| 82 | Inbox Stats Scoped to Member Accounts — FIXED 2026-04-19 | `pg-storage.ts` `getInboxStats()` — new optional `accountIds?: string[]` param adds `AND "emailAccountId" IN (...)` to all count queries. `routes.ts` `/api/inbox/enhanced` and `/api/inbox/stats` pass member's own account IDs for non-admin roles; admins see org-wide. Fixes members seeing admin org-wide pill counts (All/Unread/Need Reply). |
| 83 | Dashboard + Scorecard Reply Quality Enrichment — ADDED 2026-04-19 | `routes.ts` `/api/my/dashboard` nudge for "Emails Need Reply" now shows `🔥 N Hot · ⚡ N Warm` breakdown using `replyQualityLabel`. `/api/my/emails-needing-reply` sorts by `replyQualityScore DESC` (hot first), returns `replyQualityScore`/`replyQualityLabel` per email. `/api/team/scorecard` adds `notRepliedHot`/`notRepliedWarm` per member and teamTotals. Frontend: `my-dashboard.tsx` shows quality badge per email in expanded list; `team-scorecard.tsx` shows 🔥/⚡ pills in Not Replied cell and subtitle on summary card. |

| 84 | Bounce Reset + Surge Detection — ADDED 2026-04-21 | `routes.ts` reset endpoints (`/reset-bounces-preview`, `/reset-bounces`, `/retry-after-unblock`, `/bounce-surge`), `campaign-engine.ts` rolling-window surge detector. See `docs/bounce.md` for full logic. `isFalseBounceError()` auto-resets only auth/token failures and explicit `5.7.0`/`5.7.26`/throttle/quota codes. NDR-prefix bounces (`"Bounce: ..."` / `"Bounce detected: ..."`) require force-mode with explicit user confirmation — they are ambiguous (could be policy block OR true hard bounce). Force-mode unsuppresses contacts; only use when sender is unblocked AND addresses are verified valid. |
| 85 | Team Scorecard — Not Replied filter aligned with Inbox — FIXED 2026-04-21 | `routes.ts` `/api/team/scorecard` notReplied query — added `ui.status != 'replied' AND ui."repliedBy" IS NULL` to both the per-member count and the drilldown list. Previously only checked `repliedAt IS NULL`, missing contacts already marked replied by a team member. Same fix applied to `/api/team/scorecard/drilldown` `not_replied` type. |
| 86 | Team Scorecard — Meetings Clickable — ADDED 2026-04-21 | `team-scorecard.tsx` — Meetings count cell is now a clickable button (same pattern as Not Replied / Hot Leads). Opens drilldown modal showing Contact, Subject, Platform, Meeting time, and Join link. Backend `meetings` type already existed in `/api/team/scorecard/drilldown`. `openDrilldown` function signature updated to include `'meetings'` type. |
| 87 | Team Scorecard — Hot Leads count fixed — FIXED 2026-04-21 | `routes.ts` `/api/team/scorecard` hotLeads query — replaced old query (required both `lead_opportunities` row AND `replyType='positive'` in unified_inbox) with direct `lead_opportunities` query matching via `emailAccountId` OR `accountEmail` OR assigned contacts. `accountEmail` defaults to `''` in old records so now also matches via `emailAccountId IN (SELECT id FROM email_accounts WHERE "userId" = ?)`. Same fix in drilldown. |
| 88 | Team Scorecard — Hot Leads drilldown with AI details — IMPROVED 2026-04-21 | `team-scorecard.tsx` hot_leads drilldown modal — now shows AI Bucket badge (hot_lead/warm_lead/past_customer coloured), AI reasoning snippet, Suggested Action, Pipeline Stage, Deal Value. Drilldown query pulls from `lead_opportunities` directly (not via unified_inbox join). |
| 89 | Scorecard Emails Sent — warmup excluded — FIXED 2026-04-21 | `routes.ts` `/api/team/scorecard` emailsSent query — changed from `messages JOIN contacts WHERE c."assignedTo"=userId` to `messages JOIN email_accounts WHERE ea."userId"=userId`. Warmup sends never write to `messages` table so already excluded. Internal recipient filter still applied. Falls back to contacts-based query if `emailAccountId` column missing. |
| 90 | Daily Playbook (Task Queue) — ADDED 2026-04-21 | New `client/src/components/daily-task-queue.tsx` component on My Dashboard. Shows: (1) Today's Progress — 4 progress bars (Emails/Calls/Meetings/WhatsApp) with admin-configurable targets from `api_settings` key `daily_activity_targets`; (2) Quick log buttons for Call/Meeting/WhatsApp; (3) Priority Actions — 3 tabs: Reply Now (hot inbox replies), Overdue (contacts past nextActionDate), Follow Up (stale pipeline contacts 3d+ no activity). Log buttons mark done and refresh. |
| 91 | Daily Playbook — Log Activity endpoint — ADDED 2026-04-21 | `routes.ts` `POST /api/my/log-activity` — inserts into `contact_activities` table. `contactId` is `NOT NULL` in schema so uses `'__none__'` placeholder for quick logs without a specific contact (Log Call/Meeting/WhatsApp without selecting a contact). Uses `new Date().toISOString()` for `createdAt`. `/api/my/task-queue` GET endpoint returns hotReplies/overdueFollowups/staleLeads + todayProgress + targets. |
| 92 | Lead Intelligence Analyze — 0 contacts bug fixed — FIXED 2026-04-21 | `lead-intelligence-engine.ts` — PostgreSQL lowercases unquoted SELECT aliases (`LOWER("fromEmail") as contactEmail` returned rows keyed `contactemail`). Fixed by double-quoting all aliases in 3 queries: contactRows (`"contactEmail"`, `"contactName"`, `"accountEmail"`, `"emailAccountId"`, `"totalReceived"`, `"lastEmailDate"`), sentRows (`"contactEmail"`), inboxRows (`"contactEmail"`, `"contactName"`). GROUP BY and ORDER BY updated to match quoted aliases. |
| 93 | My Dashboard Need Reply — filter aligned with Inbox — FIXED 2026-04-21 | `routes.ts` `/api/my/dashboard` nudge count query and `/api/my/emails-needing-reply` — added `ui."repliedBy" IS NULL` and thread-level native-reply exclusion (subquery checking if a later message in same threadId came from an org email). Aligns with Unified Inbox `not_replied` filter in `pg-storage.ts`. |
| 94 | Campaign totalRecipients shrink-on-resume bug — FIXED 2026-04-22 | `campaign-engine.ts startCampaign()` — `totalRecipients` was being overwritten to `contacts.length` AFTER the "skip already-processed" filter, so resuming a paused campaign would shrink the displayed total to just the remaining work (e.g. 1159-contact list → 303 after 819 sent). Fix: track `alreadyProcessedCount` and set `totalRecipients = Math.max(existing, remaining + alreadyProcessed)` so the value can only grow. `routes.ts` `GET /api/campaigns` list-enrichment and `GET /api/campaigns/:id/detail` auto-heal existing affected campaigns by using `contactIds.length` as an additional floor when it exceeds stored `totalRecipients`. |
| 95 | Lead-Intelligence Suppression Sweep — ADDED 2026-04-22 | `lead-intelligence-engine.ts` new `sweepSuppressionSignalsFromHistory()` runs as Step 1b inside `runFullLeadIntelligence` (after scan, before analyze). Classifies historical `email_history` rows via `classifyReply`; rows with bounce confidence ≥0.9 or unsubscribe confidence ≥0.85 are added to `suppression_list` AND the matching contact (if any) is flipped via `markContactBounced`/`markContactUnsubscribed`. Guards: `isSafeEmailToSuppress()` skips connected sender/warmup accounts and system fragments (`mailer-daemon`, `postmaster`, `noreply`, etc.). Dedupes against existing suppression rows. |
| 96 | Do Not Contact — Manual Inbox Action — ADDED 2026-04-22 | `POST /api/inbox/:id/do-not-contact` in `routes.ts` — manual suppression trigger from unified inbox. Validates sender email, guards against suppressing own org accounts, adds to suppression_list with source `manual-inbox`, flips matching contact to `bounced` or `unsubscribed`, logs activity. Frontend `unified-inbox.tsx` shows red `Ban` icon button "Do not contact" only for replies classified as negative/unsubscribe/bounce. |
| 97 | Bounce Reconcile Boot Sweep — ADDED 2026-04-22 | `server/index.ts` 60s-after-boot sweep joins `messages.status='bounced'` to contacts where `status != 'bounced' AND status != 'unsubscribed'`, filters to 5000 rows, flips via `markContactBounced(id, 'hard')` which also adds to suppression_list. Protected-email guard via union of `email_accounts ∪ warmup_accounts`. Runs once per boot. |
| 98 | Unsubscribe Reconcile Boot Sweep + 6hr Scheduler — ADDED 2026-04-22 | `server/index.ts` — two sweeps join `unified_inbox.replyType='unsubscribe'` to contacts by `fromEmail` + `organizationId`, skip already-unsubscribed/bounced/protected, flip via `markContactUnsubscribed(id, campaignId)`. Boot sweep (75s delay, 5000 row cap) handles historical backlog; recurring sweep (120s delay, then every 6 hours, 500 row cap) catches new unsubscribe replies going forward. NO unsubscribe link injection in outgoing emails (per product direction). Manual `POST /api/contacts/:id/mark-unsubscribed` route remains the rep-facing path. |
| 99 | Smart Send (Engagement-based Ordering) — ADDED 2026-04-22 | Opt-in per-campaign alternative to default list-order send. Column `campaigns.sendOrder TEXT DEFAULT NULL` (NULL/`'default'` = old behavior, `'engagement'` = smart). New `applyEngagementOrdering()` helper in `campaign-engine.ts` tiers contacts by `totalReplied > 0` / `totalClicked > 0` / `totalOpened > 0` / else, then interleaves 1A:1B:2C:4D so cold contacts still land within the send window. Conditional line in `startCampaign()` runs helper only when `sendOrder='engagement'` — default campaigns unaffected. Lock enforced server-side in `PUT /api/campaigns/:id` (field stripped if campaign not in `draft` status). Radio buttons in `campaign-creator.tsx` settings panel, "Smart" badge in `campaign-detail.tsx` title. |
| 100 | OAuth Reauth Flagging + UI — ADDED 2026-04-22 | Detects repeated `invalid_grant` / `AADSTS50173` / `AADSTS70000` / `AADSTS700082` OAuth failures and flags the account for manual reconnection without breaking any send/track/campaign path. Schema: 4 columns on `email_accounts` (`authStatus` TEXT DEFAULT 'active', `authFailureCount` INTEGER DEFAULT 0, `authLastFailureAt` TEXT, `authLastErrorCode` TEXT) + `idx_email_accounts_auth_status` index, added via idempotent `ALTER TABLE ADD COLUMN IF NOT EXISTS` in `pg-storage.ts` + direct columns in `scripts/pg-schema.sql` for fresh installs. Helper: `server/services/auth-health.ts` exports `recordAuthFailure(orgId, email, rawError)` and `recordAuthSuccess(orgId, email)` — both wrapped in try/catch at top level, callers invoke with `.catch(() => {})` so they can never throw. Detector `isInvalidGrantError()` checks message text + nested `response.data.error` + `cause.message`. Threshold: 3 consecutive failures flips `authStatus` to `'reauth_required'` atomically via a single CASE expression (same UPDATE also increments count and records timestamp + code). Success call only clears when previously non-default (avoids churn). Wired into `gmail-reply-tracker.ts getValidAccessToken` (success after token set, failure in refresh catch), `outlook-reply-tracker.ts refreshAccessToken` (success before return, failure after log), `warmup-engine.ts` Gmail path (~L90) and Outlook path (~L150) including new `else` branch on non-OK refresh response. NO skip logic — existing 401 retry + force-refresh + send-path behavior unchanged; helper only observes and records. API: `GET /api/email-accounts/auth-health` returns `{needsReauth: [{id,email,provider,authFailureCount,authLastFailureAt,authLastErrorCode}], count}` scoped to caller (members see only their own accounts); existing `GET /api/email-accounts` extended with same 4 fields. UI: `ReauthBanner` component in `mailmeteor-dashboard.tsx` polls `/auth-health` every 60s, renders null when count=0, amber banner with Reconnect button when >0 (navigates to setup view). Email Accounts page (`email-account-setup.tsx`): prominent amber "Reauth Required" badge next to Active badge in header row (clickable, routes to `/api/auth/gmail-connect` or `/api/auth/outlook-connect`), detail line shows "Reauth required (N failed refreshes)", amber "Reconnect" button in actions column. All reconnect flows reuse existing OAuth routes — no new auth code. |

## Golden Rules (NEVER violate)

- **NEVER** import `server/db.ts` — crashes production (`drizzle-orm` not installed, caused 1 day downtime)
- **NEVER** drop or recreate tables — use `ALTER TABLE ADD COLUMN` with try/catch only
- **NEVER** `setContacts([])` on API error — keep stale data visible (caused "No contacts" display TWICE)
- **NEVER** bypass `storage` methods for working GET endpoints — raw SQL only as enhancement after storage fetch
- **NEVER** modify `sendViaGmailAPI` return value — must return `{ success, messageId, threadId }` for threading
- **NEVER** remove `gmailThreadId` save in `sendBatched()` or lookup in `executeFollowup()`
- **NEVER** remove 401 retry + force-refresh in `sendEmail()` Gmail path in `followup-engine.ts`
- All camelCase columns in PostgreSQL raw SQL MUST be double-quoted: `"organizationId"`, `"contactId"`, `"createdAt"`, etc.
- Raw SQL only via `storage.rawGet/rawAll/rawRun` — auto-converts `?` to `$1,$2` for PG
- **PostgreSQL returns `COUNT(*)` and `SUM()` as bigint strings** — always wrap with `parseInt()` / `Number()` or JS will string-concatenate instead of add
- `email_accounts` uses `"isActive" = 1` column (NOT `status = 'active'` — that column does not exist)
- `fromEmail` in `unified_inbox` stores `"Name <email>"` format — extract email before comparing
- **Inbox Reply tab** filter must be `(status = 'replied' OR "repliedAt" IS NOT NULL)` in all 3 locations in `pg-storage.ts` — `status = 'replied'` alone shows empty Reply tab
- **Forward endpoint** must set `forwardedFrom: msg.fromEmail` — required so original sender email shows in UI
- **Bounce classifier** (`reply-classifier.ts`) non-system senders require 3+ indicators to classify as bounce — do NOT lower threshold (legitimate replies were being misclassified)
- **Campaign enrichment** in `GET /api/campaigns` — `Promise.all` async map with try/catch — do NOT remove (powers "Sent By" and "List" columns)
- **Reply classification rules** in `reply-classifier.ts` — 30+ strengthened patterns for OOO, auto_reply, positive, negative, bounce. Do NOT weaken patterns or lower thresholds. Non-system senders require 3+ indicators for bounce classification (legitimate replies were being misclassified). Do NOT change `isHumanReply()` logic which identifies actionable replies.
- **Boot reclassification** in `server/index.ts` — targets `("replyType" IS NULL OR "replyType" = '' OR "replyType" = 'general')` at 30s startup delay. Do NOT change delay timing or message limits. Do NOT remove AI reclassification fallback for borderline cases.
- **Inbox reply filter** — "Emails Need Reply" and "Not Replied" filters must exclude non-human replies: `replyType IN ('positive', 'negative', 'general')` only. Do NOT revert to include-based filters. OOO/auto_reply/bounce must always be excluded.
- **Scorecard "Not Replied" cutoff** in `routes.ts` — must enforce 3-day lookback via `notRepliedCutoff` to allow reply time. Do NOT remove cutoff or revert to all-time counts. Gives accurate signal for aged emails.
- **Email rating badge condition** in `contacts-manager.tsx` — use `contact.emailRatingUpdatedAt` (not `emailRating > 0`) to detect rated contacts. A 0/F rating is valid and must show a badge.
- **`messages.recipientEmail` backfill** in `pg-storage.ts initializeSchema()` — runs after schema COMMIT using `queryOne`/`execute` (not `client`). Backfills from `contacts` table where `contactId` still resolves. Do NOT remove — required for rating fallback on re-imported contacts.
- **Batch rating is fire-and-forget** — `POST /api/contacts/batch-rating` responds immediately with `background: true`, then runs `batchRecalculateRatings()` via `setImmediate()`. Do NOT make it synchronous (causes HTTP timeout on large orgs).
- **autoPaused flag** — `campaigns.autoPaused` MUST be set to `true` on ALL system-initiated pauses (window closed, daily limit, sendBatched crash) and `false` on ALL user-initiated pauses (UI pause/stop). Boot recovery SQL filter depends on this being correct. Do NOT change `autoPaused` logic in `campaign-engine.ts` or `routes.ts` pause/stop endpoints.
- **resumeActiveCampaigns two-pass** — Pass 1 (`status='active'`) and Pass 2 (`status='paused' AND autoPaused=true AND autopilot.enabled=true`) are both required. Do NOT collapse into one query or remove either pass. Pass 2 recovers stranded campaigns after server restart.
- **resumeActiveCampaigns boot stagger** — 2s `await` delay after each `startCampaign()` call in both passes. Do NOT remove. Prevents simultaneous contact loading from causing heap OOM on boot with many campaigns.
- **contactIds zero-contact abort** — In `startCampaign()`, if `getContactsByIds()` returns 0 contacts, return error immediately. Do NOT revert to `getContacts(org, 10000, 0)` fallback — loads all org contacts (188k+) and causes OOM.
- **Follow-up tracking injection** — `followupTrackingId` must be generated BEFORE `sendEmail()` call in `executeFollowup()`. `addTrackingPixel()` + `addClickTracking()` must be applied to `personalizedContent` before sending. The same ID must be stored in the message record and tracking event. Do NOT move generation after send or skip injection.
- **Follow-up base URL** — `FollowupEngine.setPublicBaseUrl()` MUST be called in `routes.ts` alongside every `campaignEngine.setPublicBaseUrl()` call (7 sites total). Without this, `getBaseUrl()` falls back to `http://localhost:8080` on Azure (no `BASE_URL` env var), baking broken localhost URLs into tracking pixels of all Step 2+ emails. Confirmed root cause of 0 opens/clicks on follow-up steps (fixed 2026-04-19).
- **Gmail/Outlook warmup filter** — `ownEmailsSet` in `gmail-reply-tracker.ts` and `outlook-reply-tracker.ts` must be built from `email_accounts` (direct) AND `warmup_accounts` JOIN `email_accounts` (via `emailAccountId`). `warmup_accounts` has NO `email` column — must JOIN `email_accounts ea ON ea.id = wa."emailAccountId"`. Using `SELECT email FROM warmup_accounts` returns nothing and breaks the skip (root cause of 32k warmup inbox flood, fixed 2026-04-18).
- **Gmail 404 handling** — `gmailFetch()` returns `null` on 404. All callers that destructure the result must check `if (!msg) continue` first. Do NOT add 404 to the retry path.
- **Replied tab filter** — overridden 2026-04-18 to `"replyType" IN ('positive','negative','general')`. Do NOT revert to `(status='replied' OR "repliedAt" IS NOT NULL)` — showed only 1 message vs thousands of real replies.
- **Warmup inbox exclusion** — `getInboxMessagesEnhanced`, `getInboxMessageCountEnhanced`, `getInboxStats` in `pg-storage.ts` exclude warmup by sender only (`fromEmail NOT IN orgEmails`). `orgEmails` must union both `email_accounts` AND `warmup_accounts`. Do NOT revert to email_accounts-only or to both-sides (fromEmail AND toEmail) check.
- **getUnrepliedCampaignMessages 45-day cutoff** — `pg-storage.ts` — `AND m."sentAt" >= $2` with 45-day window is required. Removing reverts to full table scan (8s+ PG-SLOW). Do NOT remove or extend to all-time.
- **getInboxStats accountIds scoping** — `pg-storage.ts getInboxStats(orgId, accountIds?)` — when `accountIds` is provided, ALL count queries must include `AND "emailAccountId" IN (...)`. Routes MUST pass member account IDs for non-admin roles. Do NOT call with no accountIds for members — returns org-wide counts that leak other members' data.
- **Reply Quality auto-scorer** — `server/index.ts` 15-min sweep (`batchScoreOrgReplies`) runs 90s after boot. Do NOT remove — without it, `replyQualityScore` never populates and dashboard hot/warm breakdowns show zeros. Rule-based fallback ensures Azure outage doesn't block scoring.
- **Reply quality SQL uses FILTER syntax** — `COUNT(*) FILTER (WHERE "replyQualityLabel" = 'Hot')` requires PostgreSQL 9.4+. Do NOT rewrite to `CASE WHEN` subqueries unless targeting SQLite compat.
- **Warmup DB purge safety** — `warmup-inbox-purge.ts` DELETE must include `replyType NOT IN ('positive','negative','general')` guard. Do NOT remove — prevents real human replies from being purged even if sender/recipient are both org accounts.
- **Bounce/Unsub reconcile guards** — boot sweeps in `server/index.ts` MUST build `protectedEmails` set from `email_accounts ∪ warmup_accounts` before flipping any contact. Skip any contact whose email is in the protected set. Removing this causes own sender accounts to be flipped bounced/unsubscribed and excluded from campaigns. Same guard pattern required in `lead-intelligence-engine.ts` `sweepSuppressionSignalsFromHistory` via `getOrgProtectedEmails()` + `isSafeEmailToSuppress()`.
- **Unsubscribe auto-flip cadence** — 6 hours (NOT 15 min). Human unsubscribe replies arrive at human pace; 15 min was overkill. Suppression_list is checked on every campaign send regardless, so the in-between window is covered. Do NOT drop cadence below 1 hour without re-evaluating PG load.
- **No outgoing unsubscribe link injection** — per product direction, AImailPilot does NOT inject `{{unsubscribe_url}}` or `List-Unsubscribe` headers into campaign emails. Only suppression via reply classification + manual admin action. Do NOT add automatic unsubscribe link logic unless product direction changes.
- **Smart Send lock** — `campaigns.sendOrder` is read-only once campaign leaves `draft` status. `PUT /api/campaigns/:id` strips `sendOrder` from body when `campaign.status !== 'draft'`. Do NOT allow mid-campaign toggling — breaks resume ordering expectations.
- **Smart Send default** — `sendOrder` defaults to NULL (= old list-order behavior). Do NOT change the default to `'engagement'` without feature validation. Users opt in per campaign via radio button.
- **applyEngagementOrdering placement** — must run AFTER unsubscribed/bounced/suppression filters and BEFORE the "already-processed-contactIds" dedup in `startCampaign()`. Moving it elsewhere breaks dedup consistency or re-sorts sent contacts on resume.
- **Warmup inbox cleanup** — `warmup-inbox-cleanup.ts` only removes INBOX label from messages already carrying `AImailPilot-Warmup` label. Never touches real client mail. Do NOT add deletions to this service.
- **Auth-health helper is fail-open** — `recordAuthFailure` / `recordAuthSuccess` in `server/services/auth-health.ts` MUST stay wrapped in top-level try/catch, and every caller (gmail-reply-tracker, outlook-reply-tracker, warmup-engine) MUST invoke them with `.catch(() => {})`. Do NOT add any code path that diverts token refresh, skips sends, or pauses polling based on `authStatus`. The system only records + surfaces UI — user action (Reconnect button) is the only way an account clears. Adding skip/pause logic here would break send/tracking for accounts with transient network blips and is the reason this module was intentionally scoped minimal.
- **Auth-health threshold** — `REAUTH_THRESHOLD=3` in `auth-health.ts`. Count increments on any `invalid_grant` / `AADSTS50173` / `AADSTS70000` / `AADSTS700082`. Do NOT lower to 1–2 (transient network errors would cause false flags) or raise above 5 (delays visibility). `recordAuthSuccess` clears count only when previously non-default — avoids churn UPDATE on every healthy refresh.
- **Daily limit auto-resume polling — CONFIRMED WORKING 2026-04-24** — `resumeDailyLimitPausedCampaigns()` in `server/index.ts`. Called (1) immediately after `resetDailySentAll()` at UTC midnight and (2) on a 15-minute `setInterval`. Finds campaigns with `status='paused' AND autoPaused=true` that have at least one org email account with `dailySent < dailyLimit`, then calls `startCampaign()` for each with 2s stagger. Production verified: auto-paused campaigns updated exactly 15 min after pause (10:10 → 10:25 UTC), confirming poll fires on schedule. Do NOT remove either call site or the stagger — removing midnight call means delay until next 15-min tick; removing stagger risks simultaneous contact load OOM.
- **Scheduled campaign boot recovery — CONFIRMED WORKING 2026-04-24** — 15s-after-boot `setTimeout` in `server/index.ts`. Queries `status='scheduled' AND scheduledAt <= now`, calls `startCampaign()` for each overdue campaign with 2s stagger. Production verified: zero campaigns stuck in `scheduled` status after deploy. Recovers campaigns whose `scheduledAt` passed while server was down. Does NOT re-queue campaigns with future `scheduledAt` — those remain stuck until boot recovery catches them post-deadline.
- **totalRecipients step-0-only fix — CONFIRMED WORKING 2026-04-24** — `pg-storage.ts getCampaignMessageStats()` now returns `step0Sent` + `step0Bounced` fields (step-0 messages only). All three auto-heal locations in `routes.ts` (`GET /api/campaigns`, `GET /api/campaigns/:id/detail`, InboxSync) now compute `step0Floor = max(step0Sent + step0Bounced, contactIds.length)` as `totalRecipients`. Production verified: 12/12 active/following_up campaigns show `totalRecipients = contactIds.length` exactly (no inflation from follow-up sends). Do NOT revert to `stats.sent + stats.bounced` (all-steps) — was showing 1767 instead of 886 for a 1159-contact campaign.
- **checkFollowupCompletion step0Count bounce fix — CONFIRMED WORKING 2026-04-24** — `followup-engine.ts checkFollowupCompletion()` step0Count query filters `AND status = 'sent'`. Bounced step-0 messages are excluded because `processCampaignFollowups()` never creates executions for bounced contacts — counting them inflated `totalExpected` beyond what was achievable, permanently stranding 13 campaigns in `following_up`. Production verified: `test AGBA 2026 Nominations Open —` (0 step-0 sent, was permanently stuck) correctly transitioned to `completed` on first engine cycle after deploy. Do NOT remove the `status = 'sent'` filter.
- **Known monitoring item (2026-04-24)** — `BA_AI Panel Host` campaign: `autoPaused=true` since 2026-04-22, `sentCount=345/393`. Daily limit poll finds 91 org accounts with capacity but `updatedAt` has not moved — `startCampaign` may be encountering a silent error (OAuth token expiry on assigned account, or immediate re-pause). Check Azure logs for `[DailyLimitResume] Resuming campaign: BA_AI Panel Host` to confirm attempt and error. Not blocking other campaigns.
| 101 | Overnight Sending Window Support — CONFIRMED WORKING 2026-04-24 | `campaign-engine.ts` `checkSendingWindow()` + `msUntilNextSendWindow()` — campaigns configured with cross-midnight windows (e.g. 17:00–07:00 IST for India teams sending to USA daytime) now correctly detect the overnight span. `isOvernightWindow` gate: `startTime > endTime`. In-window check uses OR logic: `currentTime >= startTime \|\| currentTime < endTime`. Gap period (e.g. 07:00–17:00) is blocked with `pauseUntilMs` pointing to today's startTime. `msUntilNextSendWindow` has a matching same-day branch: detects gap via `currentTime >= endTime && currentTime < startTime` and returns ms until today's startTime. Log messages: `"OK — within overnight window"` / `"BLOCKED — overnight gap (XX:XX not in HH:MM–HH:MM window)"`. Do NOT remove overnight detection logic — normal windows (startTime < endTime) are completely unaffected (gate never triggers). |
